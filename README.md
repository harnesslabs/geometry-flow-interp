# Geometric Structure of Flow Matching

## Getting Started
```bash
# install dependencies
brew install uv
uv sync

# train default model (ignore wandb)
uv run src/main.py --offline

# generate samples
uv run src/generate.py --experiment default
```
